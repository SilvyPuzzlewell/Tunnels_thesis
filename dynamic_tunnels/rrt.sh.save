#!/bin/bash
if [ -d "$DIRECTORY" ]; then
  # Control will enter here if $DIRECTORY exists.
firm -r ./found_tunnels/tunnels0

rm -r ./found_tunnels/tunnels0
mkdir ./found_tunnels/tunnels0

cd ./found_tunnels/tunnels0
date > date_pre.txt
cd ~/Bak/dynamic_tunnels

./rrt
cd ./found_tunnels/tunnels0
date > date_post.txt
cd ~/Bak/dynamic_tunnels
